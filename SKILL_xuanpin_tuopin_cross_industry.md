---
name: xuanpin-tuopin-cross-industry
description: "跨行业选品拓品通用分析 Skill。当用户需要对任意行业（食饮/美护/3C/家居/服饰等）进行节点性选品拓品分析时使用——基于第三方消费榜(友望/蝉妈妈等) + 内部投放数据(adata等)的双源对比，产出市场缺口识别 + 赛道机会评级 + 三级商品下钻 + 双版HTML报告。核心关键词：选品拓品、跨行业赛道分析、市场缺口、供需Benchmark、三层分析链、拓品榜单。完整覆盖「行业建模→赛道分桶→三源两层对比→供需缺口判断→历史季节验证→最终主榜+HTML」全流程。"
---

# 选品拓品分析 · 跨行业通用 Skill

> **⚠️ 核心定位**：
> 本 Skill 是一套**行业无关的方法论框架**，可迁移到美护、食饮、3C 数码、家居百货、服饰鞋包等任意行业。
> 美护（个护·家清·亲子）是首个参考实现，详见 `SKILL.md`。
> Agent 在新行业的首次运行时需要先完成「行业建模」，再进入标准分析链。

---

## 一、方法论总览

```
                 ┌─────────────────────────┐
                 │     选品拓品分析链路      │
                 └───────────┬─────────────┘
                             │
         ┌───────────────────┼───────────────────┐
         ▼                   ▼                   ▼
   ┌──────────┐       ┌──────────┐       ┌──────────┐
   │ 第一层    │       │ 第二层    │       │ 第三层    │
   │ 消费端需求 │  ──▶  │ 我方供给   │  ──▶  │ 历史验证   │
   └──────────┘       └──────────┘       └──────────┘
   平台A × 平台B       内部投放数据        历史同期数据
   结构对比+偏好       规模+动量+效率      赛道基础+季节信号
         │                   │                   │
         └───────────────────┼───────────────────┘
                             ▼
         ┌─────────────────────────────────────────┐
         │  四类最终结论：缺口 / 已布局 / 饱和 / 观察   │
         │  + 三级商品下钻 + 双版 HTML               │
         └─────────────────────────────────────────┘
```

**核心原则**：不只看一个平台、不只看我方投放、不只用固定阈值。三层证据交叉验证后定结论。

---

## 二、新行业接入 · 行业建模（首次运行必须完成）

Agent 在首次接触一个新行业时，**不能直接跑分析**，必须先完成以下建模步骤：

### Step 1 · 确认行业范围

向用户确认：
- 哪些一级行业纳入分析？（如「个护 + 家清 + 亲子」或「食饮全品类」）
- 哪些行业明确排除？（如「不含美妆护肤」「不含宠物」）
- 数据覆盖周期？（如「近 4 周」或「6—8 月夏季窗口」）

### Step 2 · 建立赛道桶

**赛道桶 = 本分析的基本对比单元**，数量建议 8—15 个（太少缺粒度、太多失焦点）。

建桶四条规则：
1. **以内部投放侧（adata）类目体系为基准**，保证供给侧有数据可查。
2. **每个桶覆盖一个可独立做经营决策的品类层级**（如「头发洗护造型」而非「个护」）。
3. **多源数据（友望/蝉妈妈等）按各自平台类目或商品名关键词归入同一套桶**。
4. **建桶即冻结**。分析过程中不随意增减桶；如需调整，先更新桶字典再重跑全链路。

产出物：`references/buckets_{行业}_v1.json`
```json
{
  "_行业": "美护",
  "_版本": "v1",
  "buckets": [
    { "id": "oral_care",   "name": "口腔护理",     "行业": "个护" },
    { "id": "hair_care",   "name": "头发洗护造型", "行业": "个护" }
  ]
}
```

### Step 3 · 建立数据归桶映射

三类数据需要各自建立归桶规则：

| 数据源 | 归桶方式 | 产出物 |
|---|---|---|
| 平台 A（如友望） | 商品名关键词规则 + 人工抽检 | `references/platform_a_keywords_{行业}.json` |
| 平台 B（如蝉妈妈） | 三级类目路径优先 → 商品名降级 | `references/platform_b_mapping_{行业}.json` |
| 内部投放（如 adata） | 类目路径节点语义映射 | `references/internal_path_semantic_map_{行业}.json` |

**铁律**：
- 内部投放侧归桶**优先使用路径语义、禁止关键词字符匹配自动兜底**。
- 未知路径必须 `STOP_AND_REVIEW`，经用户确认后再补入映射表。
- 历史商品归桶只用类目路径，**不允许商品名中的营销词覆盖类目归属**。

### Step 4 · 验收匹配率

建桶后抽样验收：各数据源未匹配率应 < 30%。若某源未匹配过高，优先排查归桶规则漏洞，不要硬跑分析。

---

## 三、标准分析链（所有行业通用）

### 3.1 第一层 · 消费端需求（平台间结构对比）

**回答**：「消费者在两个平台分别买什么、偏好差异在哪」

| 维度 | 方法 |
|---|---|
| 当期规模 | 各平台内部按销售额加权归一化 → 排名分层（前 30% 高 / 中间 40% 中 / 后 30% 低） |
| 平台偏好 | 平台 A 占比 − 平台 B 占比 = 结构差（正 = A 更重，负 = B 更重） |
| 趋势方向 | 同比结构位置上移/下移（非严格统计同比，只描述方向） |
| 4 周稳定性 | 连续 4 周是否维持在同一规模档位 |

输出四类**需求来源标签**（互斥）：

| 标签 | 含义 | 置信度贡献 |
|---|---|---|
| **双平台验证** | 两个平台均有明确规模和稳定信号 | 最高 |
| **平台 A 验证** | 仅平台 A 有规模和信号，平台 B 弱 | 中高 |
| **平台 B 外部验证** | 仅平台 B 有规模和信号，平台 A 弱 | 中（待迁移验证） |
| **需求信号弱** | 两个平台均无明确规模或趋势 | 低 |

**⚠️ 关键：平台 B 强 ≠ 平台 A 红海/非重点。** 平台 B 的热门品类可能是平台 A 尚未引入的机会，不能直接排除。

### 3.2 第二层 · 我方供给（内部投放 vs 外部需求）

**回答**：「需求摆在面前，我方投放有没有跟上」

| 维度 | 数据源 | 方法 |
|---|---|---|
| 供给规模 | 内部投放 category 表 | 近 4 周 GMV/消耗排名 → 重供给（前 30%）/ 中供给（中间 40%）/ 轻供给（后 30%） |
| 供给动量 | 内部投放逐周数据 | 后 2 周较前 2 周 GMV 变化率 → 放量中 / 企稳 / 收缩中 |
| 投放效率 | 内部投放 category 表 | ROI 及其逐周变化 |
| 供给宽度 | 内部投放 spu 表 | 返回有效 SPU 数 + 类目主体计数加总（作为宽度代理） |
| 集中度 | 内部投放 spu 表 | CR5（头部 5 个 SPU 贡献度）→ >70% 高集中 |

**⚠️ 绝对禁止**：
- 把外部平台原始占比与内部投放原始占比直接相减或相除（分母不同，无业务含义）。
- 跨口径只能做「差集」（在不在清单里）+「同源趋势」（排名升降）。

### 3.3 第三层 · 历史季节验证

**回答**：「当前信号是短期波动还是历史规律、去年此时有没有跑过」

| 维度 | 方法 |
|---|---|
| 历史赛道排名 | 去年同期内部投放 GMV 排名 |
| 商品基础 | 去年 SPU 数、客户数 → 判断是否已有供给尝试 |
| 效率参考 | 去年同期 ROI |
| 代表商品 | Top 商品型（不展示品牌名，展示商品型） |

**历史数据使用铁律**：
- 如果数据窗口断裂（如架构切换导致中间缺失 → 美护的 7/9—8/3），**不拼接窗口**。
- 历史数据不与当期数据直接计算同比，只作结构参考。
- 历史窗口必须与当期窗口的行业范围一致，否则不能放同一趋势图。

---

## 四、最终结论体系（所有行业通用）

### 4.1 四类结论（互斥，一个赛道只属一类）

| 结论 | 必须同时满足 | 对应动作 |
|---|---|---|
| **市场缺口** | 任一有效需求来源 + 我方轻供给或收缩 + 效率不差 | 迅速拓品切入；若是单平台外部验证，先小规模引入测试 |
| **已布局** | 需求已验证 + 我方已有中高供给 | 按周度 GMV/ROI/SPU 附注「放量中 / 企稳 / 收缩中」，据此调整运营 |
| **市场饱和** | 需求不再扩张或收缩 + 供给规模和宽度高 + ROI 或单 SPU 产出连续走弱 | 管控内部预期，同时输入缺口替代品 |
| **观察验证** | 需求证据不足 / 数据窗口不足 / 效率不明确 | 小规模引入补证据，或延至下一周期再判 |

**⚠️ 饱和判定必须三类证据齐全**。仅凭「我方投放规模高」不能叫饱和。

### 4.2 主榜列结构（最低要求）

每个赛道的主榜行至少包含：

| 列 | 说明 |
|---|---|
| 优先级 | P0/P1/P2 |
| 赛道 | 赛道桶名称 |
| 最终结论 | 市场缺口 / 已布局 / 市场饱和 / 观察验证 |
| 当前状态 | 放量中 / 企稳 / 收缩中 / 待引入验证 / 补量中 |
| 需求来源 | 双平台验证 / 平台A验证 / 平台B外部验证 / 需求信号弱 |
| 历史季节信号 | 去年排名 + 代表商品型 + 客户/SPU 基础 |
| 置信度 | 高 / 中高 / 中 / 中低（附依据简述） |
| 三级机会品 | 当前值得拓的具体商品型（不超过 5 个） |
| 建议动作 | 一句话行动指引 |

---

## 五、Benchmark 方法（所有行业通用）

### 5.1 相对排名替代固定阈值

| 原来（已撤回） | 现在（通用方法） |
|---|---|
| 「任一平台占比 ≥ 10% = 热门」 | 平台内按排名分 30%—40%—30% 三层 |
| 「我方 GMV ≥ 12% = 重投」 | 近 4 周内部 GMV 排名分 30%—40%—30% |
| 「4 周标准差中位数 = 同比阈值」 | 同比只用排名方向（上移/下移）；4 周仅验稳定 |

10% 只是 N 桶均分理论值，不等于「热门/缺口/红海」。每个行业的赛道数不同，均分线自动变化，不能跨行业套用固定数字。

### 5.2 置信度规则

| 置信度 | 条件 |
|---|---|
| **高** | 当期需求 + 我方供给 + 历史信号，三类证据方向一致 |
| **中高** | 当期需求与我方供给方向一致，历史支持或不冲突 |
| **中** | 单平台需求验证，或当前与历史信号存在差异 |
| **中低** | 需求信号弱，或结论主要依赖有限供给记录与后段历史排名 |

---

## 六、对外输出规范（所有行业通用）

### 6.1 双版 HTML

| 版本 | 受众 | 内容 |
|---|---|---|
| **对外版** | 客户/合作伙伴/对外发布 | 市场侧用需求指数（最高=100）+ 排名 + 趋势方向；内部经营用等级（供给指数/重中轻/放量企稳收缩/效率高中低/宽度宽中窄）；不展示原始金额/消耗/精确客户数 |
| **对内版** | 内部运营/决策 | 完整数据，含精确 GMV/ROI/SPU/客户数，含方法论附录 |

### 6.2 HTML 技术规范

- 单文件自包含，图片 base64 内嵌、页内 Lightbox 放大
- 图表 ECharts 5.x CDN 加载
- 多周期对比图必须画虚线分割 + 背景区段色块 + 周期标签
- Modal 滚动区必须 `min-height: 0`（flex column 下不会自动触发 overflow）
- 绿涨红跌（涨 = `#00875a`，跌 = `#d92c20`），新增 = 蓝 `#1e2bfa`

---

## 七、新行业接入清单（Agent 执行检查表）

当用户说「帮我做 XX 行业选品拓品」时，按此清单逐步确认：

```
☐ 1. 行业范围确认
     ☐ 一级行业列表（纳入哪些、排除哪些）
     ☐ 数据覆盖周期

☐ 2. 赛道分桶
     ☐ 建 references/buckets_{行业}_v1.json（8—15 桶）
     ☐ 用户确认桶定义 → 冻结

☐ 3. 数据归桶映射
     ☐ 平台 A 商品名关键词规则
     ☐ 平台 B 三级类目路径映射
     ☐ 内部投放路径语义映射（人工审核，非关键词自动匹配）
     ☐ 未知路径 → STOP_AND_REVIEW → 用户补充

☐ 4. 匹配率验收
     ☐ 各源未匹配率 < 30%

☐ 5. 历史数据窗口确认
     ☐ 可用历史周期（是否完整/有无断裂）
     ☐ 行业范围是否与当期一致

☐ 6. 进入标准分析链
     ☐ 第一层：双平台需求结构
     ☐ 第二层：我方供给状态
     ☐ 第三层：历史季节信号
     ☐ 四类最终结论
     ☐ 三级商品下钻

☐ 7. 产出
     ☐ 最终 MD 草稿 → 用户确认
     ☐ 双版 HTML（对外脱敏 + 对内完整）
```

---

## 八、Agent 铁律（不可违反）

### 数据口径
- ❌ 外部平台占比与内部投放占比直接相减/相除（分母不同）
- ❌ 把两个行业范围不一致的历史窗口拼成连续趋势
- ❌ 仅凭我方投放规模高就判断「市场饱和」

### Benchmark
- ❌ 固定百分比（10%/12%/6%）作为判断阈值
- ✅ 平台内相对排名分层（30%—40%—30%）

### 归桶
- ❌ 关键词字符长度/首次命中自动归桶
- ✅ 路径节点语义映射 + STOP_AND_REVIEW 未知路径

### 结论
- ✅ 需求来源标签 + 历史季节信号必须在主榜独立成列
- ✅ 四类结论互斥，饱和需三类完整证据

---

## 九、参考实现

| 行业 | Skill 文件 | 关键产出 |
|---|---|---|
| **美护（个护·家清·亲子）** | `SKILL.md` | 10 赛道、2026 夏季榜单、对外版 + 对内版 HTML |
| **食饮（参考）** | 旧版已归档 | 11 品类、清凉节/中秋营销地图 |

美护版是本方法论的首个完整实现，包含：分桶字典 `references/buckets_saidao_v2.json`、路径语义映射 `references/adata_path_semantic_map_v1.json`、完整分析框架 `选品拓品_完整分析框架与Benchmark草稿.md`、置信度方案 `现有数据置信度与历史窗口使用方案.md`、数据口径文档 `数据源与分桶清洗逻辑_v2定稿.md`。

新行业接入时，Agent 应**参考美护版的数据结构和脚本逻辑**，但**不直接复用美护的桶定义、路径映射或结论**。每个行业从行业建模重新开始。

---

## 🚫 Agent 禁止行为

1. ❌ 跨行业套用其他行业的赛道桶或路径映射
2. ❌ 跳过行业建模直接跑分析
3. ❌ 用固定百分比阈值（10%/12%/6%）替代相对排名
4. ❌ 让用户自己跑脚本或编辑 JSON
5. ❌ 把外部平台原始占比与内部投放原始占比直接相减
6. ❌ 在历史数据窗口断裂时强行拼接
7. ❌ 仅凭「我方投放高」判市场饱和
